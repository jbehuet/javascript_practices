/*
  La fonction "getCharAt"
  retourne le caractère à une certaine position.
  Si la position demandée est inexistante, retourner une chaine vide.
  Prototype:
      str getCharAt(str, position);
*/
function getCharAt(str, position) {

}

/*
  La fonction "editAt"
  retourne un nouveau tableau avec un élément édité
  Si la position demandée est inexistante, retourner le même tableau.
  Prototype:
      str editAt(arr, position, value);
*/
function editAt(arr, position, value) {

}

/*
  La fonction "findPosition"
  retourne la position d'un élément
  Si la valeur demandée est inexistante, retourner -1.
  Prototype:
      str findPosition(arr, value);
*/
function findPosition(arr, value) {

}

/*
  La fonction "checkElement"
  retourne vrai si l'élement est dans le tableau sinon retourne faux
  Si la valeur demandée est inexistante, retourner -1.
  Prototype:
      str checkElement(arr, value);
*/
function checkElement(arr, value) {

}

/*
  La fonction "merge"
  retourne un tableau qui fusion les 2 tableaux
  Prototype:
      str merge(arr1, arr2);
*/
function merge(arr1, arr2) {

}

module.exports = { getCharAt, editAt, findPosition, checkElement, merge };
